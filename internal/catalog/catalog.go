// Package catalog defines the shared enum types (Gender, Role,
// Interest) and interest category/item relationships used by both
// the view layer (form rendering) and the match layer (scoring and
// filtering).
package catalog
