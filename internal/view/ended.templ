package view

// ChatEnded renders a notification after a chat session ends.
// When showActions is true, "Find Another" and "Block & Find
// Another" buttons are shown. Appended to the messages area
// via OOB swap.
templ ChatEnded(message string, showActions bool) {
	@appendMessage() {
		<div class="mx-auto text-center space-y-3 py-4">
			<p class="text-sm text-stone-500 dark:text-stone-400">{ message }</p>
			if showActions {
				<div class="flex items-center justify-center gap-2">
					<form ws-send class="inline">
						<input type="hidden" name="type" value="find_match"/>
						<button
							type="submit"
							id={ ElementFindAnother }
							class="rounded-md bg-amber-700 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-amber-600 focus-visible:ring-2 focus-visible:ring-amber-500"
						>
							Find Another
						</button>
					</form>
					<form ws-send class="inline">
						<input type="hidden" name="type" value="find_match"/>
						<input type="hidden" name="block" value="true"/>
						<button
							type="submit"
							id={ ElementBlockBtn }
							class="rounded-md bg-stone-600 px-4 py-2 text-sm font-semibold text-white shadow-sm hover:bg-stone-500 focus-visible:ring-2 focus-visible:ring-stone-400"
						>
							Block &amp; Find Another
						</button>
					</form>
				</div>
			}
		</div>
	}
}
